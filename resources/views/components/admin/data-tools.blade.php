@props(['importRoute'=>null,'exportRoute'=>null,'createRoute'=>null,'createLabel'=>'Add New'])<div class="toolbar-actions">@if($importRoute)<button class="btn btn-light border" data-bs-toggle="modal" data-bs-target="#importModal"><i class="ri-upload-2-line me-1"></i>Import CSV</button>@endif @if($exportRoute)<a class="btn btn-light border" href="{{ $exportRoute }}"><i class="ri-download-2-line me-1"></i>Export CSV</a>@endif @if($createRoute)<a class="btn btn-primary" href="{{ $createRoute }}"><i class="ri-add-line me-1"></i>{{ $createLabel }}</a>@endif</div>@if($importRoute)<div class="modal fade" id="importModal" tabindex="-1"><div class="modal-dialog modal-dialog-centered"><form method="post" enctype="multipart/form-data" action="{{ $importRoute }}" class="modal-content">@csrf<div class="modal-header"><h6 class="modal-title">Import CSV</h6><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><label class="form-label">CSV file</label><input class="form-control" type="file" name="file" accept=".csv,text/csv" required><div class="text-muted mt-2" style="font-size:11px">Use the exported CSV headers as your import template. Invalid or incomplete rows are skipped.</div></div><div class="modal-footer"><button class="btn btn-light border" type="button" data-bs-dismiss="modal">Cancel</button><button class="btn btn-primary">Import</button></div></form></div></div>@endif