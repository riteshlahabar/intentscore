@extends('admin.layouts.app')@section('title','Clients')@section('page_title','Clients')@section('page_subtitle','Manage companies and contacts with reusable filters and CSV tools.')@section('page_actions')<x-admin.data-tools :import-route="route('admin.clients.import')" :export-route="route('admin.clients.export',request()->query())" :create-route="route('admin.clients.create')" create-label="Add client"/>@endsection @section('content')<form class="filter-panel" method="get"><div class="row g-2"><div class="col-lg-4"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Search company, contact, email or phone"></div><div class="col-lg-2"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['prospect','active','inactive','won','lost'] as $x)<option value="{{ $x }}" @selected(request('status')===$x)>{{ ucfirst($x) }}</option>@endforeach</select></div><div class="col-lg-2"><input class="form-control" name="city" value="{{ request('city') }}" placeholder="City"></div><div class="col-lg-2"><input class="form-control" type="date" name="date_from" value="{{ request('date_from') }}"></div><div class="col-lg-2 d-flex gap-1"><button class="btn btn-primary flex-fill">Filter</button><a class="btn btn-light border" href="{{ route('admin.clients.index') }}">Reset</a></div></div></form><div class="card table-card"><div class="table-responsive"><table class="table"><thead><tr><th>Company</th><th>Contact</th><th>Location</th><th>Owner</th><th>Status</th><th>Created</th><th></th></tr></thead><tbody>@forelse($clients as $c)<tr><td><strong>{{ $c->company_name }}</strong><div class="stat-mini">{{ $c->email ?: '—' }}</div></td><td>{{ $c->contact_name ?: '—' }}<div class="stat-mini">{{ $c->phone ?: '—' }}</div></td><td>{{ collect([$c->city,$c->state])->filter()->join(', ') ?: '—' }}</td><td>{{ $c->owner?->name ?: '—' }}</td><td><x-admin.status-badge :value="$c->status"/></td><td>{{ $c->created_at->format('d M Y') }}</td><td class="text-end"><a class="action-btn" href="{{ route('admin.clients.edit',$c) }}"><i class="ri-edit-line"></i></a><form class="d-inline" method="post" action="{{ route('admin.clients.destroy',$c) }}">@csrf @method('DELETE')<button class="action-btn" data-confirm="Move this client to trash?"><i class="ri-delete-bin-line"></i></button></form></td></tr>@empty<tr><td colspan="7"><div class="empty-state"><i class="ri-building-line"></i><div class="mt-2">No clients match your filters.</div></div></td></tr>@endforelse</tbody></table></div><div class="px-3 pb-2">{{ $clients->links() }}</div></div>@endsection