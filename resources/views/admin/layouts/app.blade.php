<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>@yield('title','Admin') - {{ \App\Models\Setting\Setting::value('company_name','Sales Portal') }}</title><link rel="icon" href="{{ asset('admin-assets/images/favicon.png') }}"><link rel="stylesheet" href="{{ asset('admin-assets/css/vendors/bootstrap.css') }}"><link rel="stylesheet" href="{{ asset('admin-assets/css/remixicon.css') }}"><link rel="stylesheet" href="{{ asset('css/portal-admin.css') }}">@stack('styles')</head><body><div class="portal-shell">@include('admin.partials.sidebar')<header class="portal-header"><button class="btn btn-light mobile-toggle" data-sidebar-toggle><i class="ri-menu-line"></i></button><form class="top-search" action="{{ route('admin.search') }}" method="get"><i class="ri-search-line"></i><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Search clients, leads, products or presentations..." autocomplete="off"></form><div class="header-right"><a class="btn btn-outline-primary d-none d-sm-inline-flex" href="{{ route('admin.presentations.create') }}"><i class="ri-add-line me-1"></i> New presentation</a><div class="user-pill"><div class="user-avatar">{{ strtoupper(substr(auth()->user()->name,0,1)) }}</div><div class="user-meta"><div class="fw-semibold">{{ auth()->user()->name }}</div><div class="text-muted" style="font-size:10.5px">{{ ucwords(str_replace('_',' ',auth()->user()->role)) }}</div></div></div><form action="{{ route('admin.logout') }}" method="post">@csrf<button class="btn btn-light" title="Logout"><i class="ri-logout-box-r-line"></i></button></form></div></header><main class="portal-main"><div class="d-flex align-items-start justify-content-between mb-3"><div><h1 class="page-title">@yield('page_title')</h1><div class="page-subtitle">@yield('page_subtitle')</div></div>@yield('page_actions')</div>@if(session('success'))<div class="alert alert-success py-2">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert alert-danger py-2"><strong>Please correct:</strong><ul class="mb-0 mt-1">@foreach($errors->all() as $e)<li>{{ $e }}</li>@endforeach</ul></div>@endif @yield('content')</main></div><script src="{{ asset('admin-assets/js/bootstrap/bootstrap.bundle.min.js') }}"></script><script src="{{ asset('js/portal-admin.js') }}"></script>@stack('scripts')</body></html>