@extends('admin.layouts.app')@section('title','Products')@section('page_title','Products')@section('page_subtitle','Reusable software catalogue powering every client presentation.')@section('page_actions')<x-admin.data-tools :import-route="route('admin.products.import')" :export-route="route('admin.products.export',request()->query())" :create-route="route('admin.products.create')" create-label="Add product"/>@endsection @section('content')<form class="filter-panel" method="get"><div class="row g-2"><div class="col-lg-5"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Search product, category or tagline"></div><div class="col-lg-3"><select class="form-select" name="category"><option value="">All categories</option>@foreach($categories as $x)<option value="{{ $x }}" @selected(request('category')===$x)>{{ $x }}</option>@endforeach</select></div><div class="col-lg-2"><select class="form-select" name="status"><option value="">All status</option><option value="active" @selected(request('status')==='active')>Active</option><option value="inactive" @selected(request('status')==='inactive')>Inactive</option></select></div><div class="col-lg-2 d-flex gap-1"><button class="btn btn-primary flex-fill">Filter</button><a class="btn btn-light border" href="{{ route('admin.products.index') }}">Reset</a></div></div></form><div class="card table-card"><div class="table-responsive"><table class="table"><thead><tr><th>Product</th><th>Category</th><th>Base price</th><th>Features</th><th>Demos</th><th>Status</th><th></th></tr></thead><tbody>@forelse($products as $p)<tr><td><div class="d-flex align-items-center gap-2">@if($p->cover_image)<img class="preview-cover" src="{{ asset($p->cover_image) }}">@endif<div><strong>{{ $p->name }}</strong><div class="stat-mini">{{ \Illuminate\Support\Str::limit($p->tagline,70) }}</div></div></div></td><td>{{ $p->category ?: '—' }}</td><td>{{ $p->base_price?'₹'.number_format((float)$p->base_price):'—' }}</td><td>{{ $p->features_count }}</td><td>{{ $p->demo_links_count }}</td><td><x-admin.status-badge :value="$p->status"/></td><td class="text-end"><a class="action-btn" href="{{ route('admin.products.edit',$p) }}"><i class="ri-edit-line"></i></a></td></tr>@empty<tr><td colspan="7"><div class="empty-state">No products found.</div></td></tr>@endforelse</tbody></table></div><div class="px-3 pb-2">{{ $products->links() }}</div></div>@endsection